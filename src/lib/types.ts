// src/lib/types.ts

export type InvitationEmailData = {
  name: string; // Store or team name
  inviteLink: string; // Unique link for the invite
  inviter: string; // Name of the person sending the invite
};
